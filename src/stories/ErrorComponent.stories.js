import ErrorComponent from "../components/ErrorComponent/ErrorComponent";

export default {
    title: 'Error Component',
    component: ErrorComponent,
};

const Template = args => <ErrorComponent />;

export const Default = Template.bind({});